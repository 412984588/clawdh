# Prisma Patterns Dev Pack — Gumroad Listing

**Title:** Prisma Patterns Dev Pack — 10 TypeScript ORM Templates

**Short description:** Production-ready Prisma patterns: transactions, soft delete, pagination, raw queries, Client Extensions, and testing — with full TypeScript.

**Long description:**

Most Prisma tutorials teach you `prisma.user.findMany()`. This pack shows you what comes next.

**What's included:**

✅ Basic CRUD — create/update/upsert/delete with type-safe args
✅ Relations — nested create, connectOrCreate, include vs select
✅ Migrations — dev/deploy workflow, seeding, safe column renames
✅ Filtering — where operators, OR/AND/NOT, relation filters, dynamic builder
✅ Transactions — sequential array, interactive callback, retry logic
✅ Soft delete — deletedAt extension auto-filters deleted records (Pro)
✅ Pagination — offset, cursor, Relay connections, keyset (Pro)
✅ Raw queries — $queryRaw with type-safety, JSONB, dynamic sort (Pro)
✅ Extensions — computed fields, model methods, query hooks, metrics (Pro)
✅ Testing — fixtures, transaction rollback, mock pattern, Vitest (Pro)

**Pricing:**
- Starter (01–05): $19
- Pro (all 10): $39

**Tags:** prisma, typescript, nodejs, orm, postgresql, database, backend
