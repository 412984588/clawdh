# FAQ — TypeScript Full Stack Bundle

**What's included?**
TypeScript Config Pack (Complete), Zod Validation Schema Templates (Pro, 10 schemas), Database Schema Templates (Complete, 13 schemas + Prisma + TypeScript types).

**Do these products work together?**
Yes — Database Schema Templates generate TypeScript interfaces that pair directly with Zod schemas for API validation, and the TypeScript Config Pack provides the tsconfig presets to compile it all.

**What databases are covered?**
PostgreSQL. Prisma schemas work with any Prisma-supported database.

**Is this a one-time purchase?**
Yes. Buy once, use forever.
