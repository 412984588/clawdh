# TypeScript Strict ESLint & Prettier Config

Maximum TypeScript strictness: type-checked rules, stylistic enforcement, no escape hatches.

## Usage
```bash
npm i -D eslint typescript-eslint @eslint/js prettier
```
Requires `tsconfig.json` with `"strict": true`.
