# Monorepo ESLint & Prettier Config

Root-level flat ESLint config for Turborepo / pnpm workspaces.
Place at repo root; each package extends or overrides as needed.

## Usage
```bash
npm i -D eslint typescript-eslint @eslint/js prettier -w
```
