# Next.js ESLint & Prettier Config

Flat ESLint config using Next.js official presets with Tailwind CSS class sorting.

## Usage
```bash
npm i -D eslint eslint-config-next prettier prettier-plugin-tailwindcss
```
Copy `eslint.config.js` and `.prettierrc` to your project root.
