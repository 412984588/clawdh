# Svelte + TypeScript ESLint & Prettier Config

Flat ESLint config for SvelteKit projects with TypeScript support.

## Usage
```bash
npm i -D eslint typescript-eslint @eslint/js eslint-plugin-svelte svelte-eslint-parser prettier prettier-plugin-svelte
```
