# Vue 3 + TypeScript ESLint & Prettier Config

Flat ESLint config for Vue 3 Composition API projects with script setup support.

## Usage
```bash
npm i -D eslint typescript-eslint @eslint/js eslint-plugin-vue vue-eslint-parser prettier
```
