# Vitest ESLint & Prettier Config

ESLint config with `eslint-plugin-vitest` for consistent, best-practice test files.
Enforces `describe` blocks, prefers `it` over `test`, and relaxes TypeScript rules in tests.

## Usage
```bash
npm i -D eslint typescript-eslint @eslint/js eslint-plugin-vitest prettier
```
