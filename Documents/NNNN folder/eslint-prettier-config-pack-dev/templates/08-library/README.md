# TypeScript Library ESLint & Prettier Config

Strict config optimised for publishable npm libraries: enforces explicit return types
and module boundaries, bans console.log in source.

## Usage
```bash
npm i -D eslint typescript-eslint @eslint/js prettier
```
