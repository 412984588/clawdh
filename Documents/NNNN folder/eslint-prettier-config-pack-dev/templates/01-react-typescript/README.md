# React + TypeScript ESLint & Prettier Config

Flat ESLint config for React + TypeScript projects with hooks and fast-refresh support.

## Usage
```bash
npm i -D eslint typescript-eslint @eslint/js eslint-plugin-react-hooks eslint-plugin-react-refresh prettier
```
Copy `eslint.config.js` and `.prettierrc` to your project root.
