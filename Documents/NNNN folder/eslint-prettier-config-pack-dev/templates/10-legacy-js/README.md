# Legacy JavaScript ESLint & Prettier Config

Classic `.eslintrc.json` (non-flat) for plain ES2021+ JavaScript projects without TypeScript.
Enforces modern JS best practices: `const`/`let`, strict equality, curly braces.

## Usage
```bash
npm i -D eslint prettier
```
