# ESLint + Prettier Config Pack

**10 ESLint & Prettier configs for TypeScript stacks. Drop in, lint immediately.**

## The Problem

Every new project means another hour fighting ESLint: picking plugins, writing rules, figuring out flat config vs legacy, getting Prettier to play nice. Then doing it again for the next project.

## The Solution

10 production-ready configs for the stacks you actually use. Copy the files, install the deps, done.

## Configs Included

**Starter ($19)** — 4 most common stacks:
- React + TypeScript (flat config, react-hooks, react-refresh)
- Next.js (official presets + Tailwind class sorting)
- Node.js API (strict TypeScript + security plugin)
- TypeScript Strict (type-checked rules, maximum enforcement)

**Pro ($39)** — All 10 configs:
- Everything in Starter, plus:
- Vue 3 + TypeScript (Composition API + script setup)
- SvelteKit + TypeScript
- Monorepo root config (Turborepo / pnpm workspaces)
- npm Library (explicit return types, no console.log)
- Vitest test suite rules (eslint-plugin-vitest)
- Legacy JavaScript (.eslintrc.json, ES2021+, no TypeScript)

## What You Get Per Config

- `eslint.config.js` (flat) or `.eslintrc.json` (legacy JS)
- `.prettierrc` with opinionated defaults
- `README.md` with install command and usage

## License

One-time purchase. Use on unlimited personal and commercial projects.
