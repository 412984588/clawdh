# ESLint + Prettier Config Pack

**10 drop-in ESLint & Prettier configs. One for every TypeScript stack.**

---

## What's Inside

**Starter — $19**
4 configs for the most common stacks: React+TypeScript, Next.js, Node.js API, TypeScript Strict.

**Pro — $39**
All 10 configs: adds Vue 3, SvelteKit, Monorepo, npm Library, Vitest, Legacy JS.

## Why Buy This

- All configs use ESLint's modern flat config format (except Legacy JS)
- Prettier integration is pre-configured — no conflicts
- Security rules included in the Node.js API config
- Vitest-specific rules for cleaner test files
- Each config is a standalone folder: copy, install deps, done

## License

One-time purchase. Unlimited projects. MIT license.
