# GraphQL Patterns — itch.io Listing

## GraphQL Patterns

**10 TypeScript templates for GraphQL developers**

A developer tool pack for API builders.

Stop writing the same GraphQL boilerplate from scratch. These patterns work.

**What's inside:**
- Schema design with scalars, interfaces, and unions
- Context-aware field resolvers
- Mutations with input validation and error unions
- PubSub subscriptions with room filtering
- DataLoader batching (goodbye N+1 queries)
- Schema auth directives (@auth, @hasRole)
- Relay cursor pagination + offset/limit
- File upload with multipart/form-data
- Apollo Federation v2 subgraphs
- GraphQL testing with executeOperation

**Tiers:**
- Starter: $19 (templates 01–05)
- Pro: $39 (all 10)

MIT licensed. Apollo Server 3/4, TypeScript 5+.
