# Stripe Webhook Handlers Pack

10 TypeScript Stripe webhook handlers. Production-ready, testable, idempotent.

Starter ($19): payment intents + subscription lifecycle (5 handlers).
Pro ($39): All 10 handlers — adds invoice events, trial reminder, checkout session, full webhook router.

Uses dependency injection so handlers are testable without mocking globals. Works with Next.js App Router, Express, Fastify. One-time purchase.
