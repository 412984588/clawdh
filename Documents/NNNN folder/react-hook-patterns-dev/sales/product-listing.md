# React Hook Patterns

**10 production-ready custom React hooks. Copy, drop in, ship.**

You've written `useDebounce` five times across five projects. You've googled "useLocalStorage SSR safe" three times this month. Stop.

This pack gives you 10 battle-tested, TypeScript-first custom hooks to drop into any React project.

---

## What's Inside

### Starter — $19
**5 hooks that save the most time**

| Hook | What it does |
|------|--------------|
| `useDebounce` | Debounce a value or callback with configurable delay |
| `useLocalStorage` | SSR-safe localStorage with JSON serialization + cross-tab sync |
| `useMediaQuery` | Reactive CSS media query + pre-built Tailwind breakpoints |
| `useIntersectionObserver` | Viewport visibility for lazy loading, infinite scroll, scroll animations |
| `useFetch` | Data fetching with loading/error/data + abort on unmount + polling |

### Pro — $39
**All 10 hooks — the complete custom hook library**

Everything in Starter, plus:

| Hook | What it does |
|------|--------------|
| `usePrevious` | Previous render value + history tracking + change detection |
| `useClipboard` | Copy to clipboard with status feedback + execCommand fallback |
| `useEventListener` | Typed global/element event listener with auto-cleanup |
| `useKeyPress` | Key hold state + hotkeys + `useHotkeyMap` for multiple shortcuts |
| `useOutsideClick` | Close dropdowns and modals on outside click/touch |

---

## Every Hook Includes

- **TypeScript-first** — fully typed, generics where needed
- **Production patterns** — abort controllers, handler refs, cleanup
- **README** — shows the API, use cases, and common gotchas

---

## Who This Is For

- React developers who keep copy-pasting the same hooks
- Teams who want a standard hook library without a heavy dependency
- Developers who prefer to own their hooks rather than import from npm

---

## Save vs Reinventing

Average: 20-60 minutes to write + test each hook correctly
This pack: copy, import, done — **2 minutes per hook**

**Starter: ~~$100 value~~ $19 | Pro: ~~$200 value~~ $39**

---

## Requirements

- React 16.8+
- TypeScript 4.9+

## License

One-time purchase. Use on unlimited personal and commercial projects.
