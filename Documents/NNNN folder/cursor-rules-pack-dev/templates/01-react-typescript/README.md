# React + TypeScript Cursor Rules

Drop-in Cursor rules for React + TypeScript projects.

## Files
- `.cursorrules` — legacy format (Cursor < 0.43)
- `.cursor/rules/main.mdc` — new MDC format (Cursor 0.43+)

## Usage
Copy both files to your project root. Cursor auto-detects them.
