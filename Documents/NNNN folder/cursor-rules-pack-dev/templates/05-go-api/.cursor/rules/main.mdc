---
description: Go API rules — idiomatic Go, error handling, project layout, testing
alwaysApply: true
---

# Go API Rules

## Error Handling
- Always check errors — never `_` on error returns
- Wrap with context: `fmt.Errorf("layer.Op: %w", err)`
- Sentinel errors for known conditions

## Interfaces
- Define interfaces where they are used (consumer side)
- Keep interfaces small (1-3 methods)

## Never
- `panic` for normal errors
- Ignore returned errors
- Global state (use dependency injection)
