# React TypeScript Cursor Rules

## 适用场景
React 18+ SPA 项目，使用 Vite + TypeScript + React Query + Zustand 技术栈。

## 核心规则摘要
- 函数式组件 + TypeScript 严格模式
- React Query 管理服务端状态，Zustand 管理客户端全局状态
- React Hook Form + Zod 处理表单
- 自定义 Hooks 封装业务逻辑

## 使用方法
将 `.cursorrules` 文件复制到你的 React 项目根目录。
