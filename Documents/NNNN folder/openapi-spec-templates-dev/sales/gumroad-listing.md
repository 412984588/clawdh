# OpenAPI Spec Templates — Gumroad

**10 OpenAPI 3.1 specs. Copy, adapt, ship.**

---

Writing OpenAPI specs from scratch is tedious. These 10 templates cover the patterns every API needs — correct syntax, proper $ref usage, reusable schemas, security definitions.

✅ REST CRUD (pagination, error schemas)
✅ Auth API (JWT, refresh, forgot/reset)
✅ E-commerce (products, cart, orders)
✅ File Upload (multipart, presigned URLs)
✅ Payment API (Stripe-compatible)
✅ Social API (posts, likes, feed)
✅ Search API (full-text, autocomplete, facets)
✅ Webhook API (endpoints, delivery logs)
✅ Admin API (user management, feature flags)
✅ Multi-Tenant (orgs, members, invitations)

**Starter** ($19): CRUD · Auth · E-commerce · File Upload
**Pro** ($39): All 10

Each spec validates with `npx @redocly/cli lint`. MIT license.
