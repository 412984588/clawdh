# Redis Patterns Dev Pack — Gumroad Listing

**Title:** Redis Patterns Dev Pack — 10 TypeScript Templates

**Short description:** Production-ready Redis patterns for TypeScript: caching, queues, pub/sub, rate limiting, sessions, distributed locks, and streams.

**Long description:**

Tired of googling "Redis sliding window rate limit TypeScript" for the fifth time?

This pack gives you 10 battle-tested templates you can drop straight into your project.

**What's included:**

✅ String ops, counters, TTL strategies
✅ Hash maps — user profiles, product catalogs, atomic increments
✅ List queues — FIFO, blocking consumers, activity feeds
✅ Sets & sorted sets — leaderboards, mutual follows, time series
✅ Pub/sub — event bus, presence detection, pattern subscriptions
✅ Caching — cache-aside, write-through, stampede prevention with mutex
✅ Rate limiting — fixed window, sliding window, token bucket (Lua)
✅ Session store — create, rotate, invalidate, force logout
✅ Distributed locks — SET NX EX, Redlock, fencing tokens, leader election
✅ Redis Streams — consumer groups, dead letter queue, event sourcing

**Pricing:**
- Starter (01–05): $19
- Pro (all 10): $39

**Tags:** redis, typescript, nodejs, backend, caching, queues, rate-limiting
