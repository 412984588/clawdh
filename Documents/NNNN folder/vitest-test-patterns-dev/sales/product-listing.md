# Vitest Test Patterns

**Stop writing boilerplate tests. 10 production-ready patterns, copy and ship.**

Every TypeScript project needs tests. But writing test boilerplate from scratch — timer mocks, fetch spies, snapshot setup, parameterized tables — wastes hours every time.

This pack gives you 10 battle-tested patterns you can drop into any project today.

---

## What's Inside

### Starter — $19
**5 essential patterns that cover 90% of unit testing**

| Template | What it teaches |
|----------|-----------------|
| `01-unit-functions` | Pure function testing, `toBe`, `toEqual`, `toThrow` |
| `02-class-testing` | `beforeEach` isolation, nested describes, lifecycle hooks |
| `03-async-promises` | `async/await`, `rejects.toThrow`, retry patterns, `Promise.allSettled` |
| `04-timer-mocking` | `vi.useFakeTimers`, debounce/throttle testing, `advanceTimersByTime` |
| `05-module-mocking` | `vi.fn`, `vi.mocked`, `mockResolvedValue`, dependency injection |

### Pro — $39
**All 10 patterns — the complete toolkit**

Everything in Starter, plus:

| Template | What it teaches |
|----------|-----------------|
| `06-component-testing` | React Testing Library patterns, `userEvent`, `waitFor`, context providers |
| `07-api-mocking` | `vi.spyOn(globalThis, 'fetch')`, HTTP error codes, `expect.objectContaining` |
| `08-snapshot-testing` | `toMatchInlineSnapshot`, when to use/avoid snapshots, update workflow |
| `09-error-handling` | Custom error classes, `toBeInstanceOf`, typed error fields |
| `10-parameterized-tests` | `it.each`, `describe.each`, named column tables |

---

## Who This Is For

- TypeScript developers who keep rewriting the same testing boilerplate
- Teams onboarding new devs who need testing examples, fast
- Developers moving from Jest to Vitest
- Anyone who wants tests that actually catch bugs, not just hit coverage %

---

## Save Hours on Every New Project

Individual research time: ~2-4 hours per pattern = 20-40 hours total
This pack: copy, adapt, run — **under 5 minutes per pattern**

**Starter: ~~$76 value~~ $19 | Pro: ~~$152 value~~ $39**

---

## Requirements

- Vitest (any recent version)
- TypeScript 4.9+
- For template 06: `@testing-library/react`, `@testing-library/user-event`

## License

One-time purchase. Use on unlimited personal and commercial projects.
