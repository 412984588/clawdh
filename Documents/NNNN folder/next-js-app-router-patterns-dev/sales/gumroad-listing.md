# Next.js App Router Patterns

**10 production-ready patterns for Next.js 14 & 15. Everything you need for the App Router.**

Stop re-reading the Next.js docs every time you start a new project. These templates cover the patterns that actually come up in production — typed, documented, and ready to copy.

## What you get

**Starter ($19) — Templates 01–05:**
- Server Components — async data fetching, parallel requests with Promise.all, Suspense streaming
- Server Actions — form handling, optimistic updates, revalidatePath/revalidateTag
- Route Handlers — GET/POST/DELETE, streaming responses, CORS headers, auth middleware
- Loading States — loading.tsx, skeleton components with Tailwind animate-pulse, granular Suspense
- Error Handling — error.tsx, global-error.tsx, not-found.tsx, programmatic notFound()

**Pro ($39) — All 10 templates:**
- Everything in Starter, plus:
- Metadata & SEO — static/dynamic metadata, OG image generation, JSON-LD, robots.txt, sitemap.xml
- Parallel Routes — @slot convention, independent streaming, intercepting routes modal pattern
- Middleware — JWT auth, locale detection, A/B testing, rate limiting, CSP security headers
- Caching Patterns — unstable_cache, revalidateTag, fetch cache options, ISR configuration
- Layout Patterns — nested layouts, route groups, template.tsx, dashboard structure

## Requirements

- Next.js 14 or 15
- TypeScript

## License

One-time purchase, unlimited projects (personal and commercial).
