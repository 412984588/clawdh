# 04 — Loading States

`loading.tsx`, Suspense boundaries, skeleton components, and `useTransition`.

## Patterns

- `loading.tsx` — automatic Suspense wrapper for route segments
- Skeleton components with `animate-pulse` (Tailwind)
- Granular Suspense boundaries — independent streaming per section
- `useTransition` — show pending state for client-side navigation
