# React Email Templates Pack

10 ready-to-use React Email templates for SaaS apps. TypeScript. Drop-in ready.

Starter pack ($19): 5 templates — welcome, verify, reset password, magic link, invoice.
Pro pack ($39): All 10 templates — adds subscription started/cancelled, team invite, payment failed, onboarding.

Built with @react-email/components. Works with Resend, SendGrid, Postmark, AWS SES, Nodemailer.

Just drop the .tsx files into your project, import, and call `render()`. Every template has typed props and a README.

One-time purchase. Use on unlimited projects.
