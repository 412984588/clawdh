# Zustand State Management Patterns Pack

10 Zustand v4 client state patterns. TypeScript. React 18+.

Starter ($19): auth, cart (persisted), UI state, multi-step forms, notifications.
Pro ($39): All Starter + settings (immer), infinite list factory, WebSocket, undo/redo, slices.

Requirements: zustand v4+, immer (for settings store), React 18+
