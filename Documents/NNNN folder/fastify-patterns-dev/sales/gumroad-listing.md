# Fastify Patterns Dev Pack — Gumroad Listing

**Title:** Fastify Patterns Dev Pack — 10 TypeScript Templates

**Short description:** Production-ready Fastify patterns: TypeBox validation, plugins, JWT auth, error handling, WebSockets, file uploads, testing, and deployment.

**Long description:**

Fastify is 2x faster than Express. This pack shows you how to use it properly.

**What's included:**

✅ Basic routes — typed GET/POST/PUT/DELETE with params and query strings
✅ Schema validation — TypeBox generates JSON Schema from TypeScript types
✅ Plugins — fp() pattern, decorators, encapsulation, module augmentation
✅ Hooks — onRequest, preHandler, onSend, request lifecycle, content type parser
✅ Authentication — JWT with refresh tokens, API key auth, role-based guards
✅ Error handling — AppError classes, global setErrorHandler, consistent format (Pro)
✅ File upload — @fastify/multipart, validation, streaming (Pro)
✅ WebSockets — rooms, broadcast, typed messages, heartbeat (Pro)
✅ Testing — inject() test client, app factory, mock dependencies (Pro)
✅ Deployment — graceful shutdown, health checks, CORS, compression, rate limiting (Pro)

**Pricing:**
- Starter (01–05): $19
- Pro (all 10): $39

**Tags:** fastify, typescript, nodejs, backend, api, rest, websockets
